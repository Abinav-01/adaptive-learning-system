# Simple placeholder Node container
FROM node:18-alpine
WORKDIR /app
# Keeps the container running so Docker Compose doesn't crash
CMD ["sh", "-c", "echo 'Frontend Container Ready' && tail -f /dev/null"]